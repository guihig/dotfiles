Variable.g({
    startify_files_number = 1,

    startify_session_persistence = 1,

    startify_lists = {
        {['type'] = 'sessions', ['header'] = {'   Saved sessions'}},
        {['type'] = 'dir', ['header'] = {'   Recent files'}}
    }
})
