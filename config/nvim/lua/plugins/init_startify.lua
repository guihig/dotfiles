vim.g.startify_files_number = 1

vim.g.startify_session_persistence = 1

vim.g.startify_lists = {
    {['type'] = 'sessions', ['header'] = {'   Saved sessions'}},
    {['type'] = 'dir', ['header'] = {'   Recent files'}}
}
